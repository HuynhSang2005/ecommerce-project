/**
 * DTO Yêu cầu Đăng ký (Register Request DTO)
 * 
 * Xác thực sử dụng class-validator:
 * - Định dạng email
 * - Độ mạnh mật khẩu
 * - Các trường bắt buộc
 */
export class RegisterRequestDto {
  // TODO: Định nghĩa DTO với các decorator xác thực
  // @IsEmail()
  // email: string;
  
  // @IsString()
  // @MinLength(8)
  // @Matches(password regex)
  // password: string;
  
  // @IsString()
  // @MinLength(2)
  // name: string;
  
  // @IsOptional()
  // @IsPhoneNumber('VN')
  // phoneNumber?: string;
}
