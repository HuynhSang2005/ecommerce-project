/**
 * DTO Trả về Thông tin User (User Response DTO)
 * 
 * Quan trọng: KHÔNG BAO GIỞ trả về domain entities trực tiếp
 * DTOs kiểm soát dữ liệu nào được lộ ra cho clients
 * 
 * Bảo mật:
 * - Không có trường password
 * - Không có các ID nội bộ không nên được lộ
 * - Chỉ có dữ liệu cần thiết
 */
export class UserResponseDto {
  // TODO: Định nghĩa response DTO
  // id: number;
  // email: string;
  // name: string;
  // phoneNumber?: string;
  // avatar?: string;
  // status: string;
  // isTwoFactorEnabled: boolean;
  // createdAt: Date;
  // role: {
  //   id: number;
  //   name: string;
  // };
}
