/**
 * Triển khai JWT Token Service
 * 
 * Triển khai (Implements) interface ITokenService
 * Sử dụng @nestjs/jwt cho việc sinh/xác thực token
 * 
 * Cấu hình:
 * - Access token: Thời gian sống ngắn (15ph - 1h)
 * - Refresh token: Thời gian sống dài (7-30 ngày)
 * - Secret từ biến môi trường (environment variables)
 */
export class JwtTokenService {
  // TODO: Triển khai service
  // Sử dụng JwtService từ @nestjs/jwt
}
